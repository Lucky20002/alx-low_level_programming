#include "main.h"
/**
 * main - determi if a number is positive, negative or zero
(* 0 : is the nnumber to be checked
 * Return: 0 success
 */
void positive_or_negative(int i)
{
if (i < 0)
{printf("%d is %s\", i, "negative");
}
else if (i < 0)
{printf("%d is %s\", i, "positive");
}
else
{
printf("%d is %s\", i, "zero");
}
return;
}
